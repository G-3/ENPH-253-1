class Robot(object):
    def __init__(curr_node, dest_node = None):
        self.curr_node=curr_node
        self.dest_node = dest_node
        self.position = self.curr_node.position()

    def setDest(dest_node):
        self.dest_node = dest_node

    def move(step):
        distance = map(sub, self.dest_node.position, self.position)
        if self.dest_node
            
